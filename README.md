# CSC 3100 Web Programming - Final Project

## Member Evaluation Application

The way this is setup, When running the index.html with liveserver you have to change the beginning of the URL from '127.0.0.1' to 'localhost' so the SessionID stored in cookies is able to be sent to the backend.

